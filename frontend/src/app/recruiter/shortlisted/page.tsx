import { ShortlistedList } from "@/components/recruiter/shortlisted/ShortlistedList";

export const metadata = {
  title: "Shortlisted Candidates | AIRecruitX HR",
  description: "View shortlisted candidates ready for the next hiring step",
};

export default function ShortlistedPage() {
  return <ShortlistedList />;
}
